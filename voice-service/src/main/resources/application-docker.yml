uniplay:
  voice:
    livekit-url: ${LIVEKIT_URL:ws://livekit:7880}
    public-url: ${LIVEKIT_PUBLIC_URL:ws://localhost:7880}
spring:
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
